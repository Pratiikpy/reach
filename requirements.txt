# Reach ASP — deep research + internet access (Claude Fable 5 on 0G)
requests>=2.31
eth-account>=0.11
fastapi>=0.111
uvicorn[standard]>=0.30
# fetch engine (Scrapling, BSD-3) + its stealth deps
scrapling>=0.4
curl_cffi>=0.7
# NOTE: also run `scrapling install` once for the stealth browser, and install the
# agent-reach CLIs (twitter-cli, rdt-cli, yt-dlp, gh) for the walled-garden channels.
